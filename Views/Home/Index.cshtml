@model List<Product>  // Укажите тип модели

@{
    ViewData["Title"] = "Products";
}

<h1>Products</h1>

@if (Model.Count == 0)
{
    <p>No products found.</p>
}
else
{
    <ul>
        @foreach (var product in Model)
        {
            <li>@product.Name</li>
        }
    </ul>
}